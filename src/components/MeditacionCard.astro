---
import type { Meditacion } from '../lib/types';
import { contarEpisodiosMeditacion } from '../lib/datos';

interface Props {
  meditacion: Meditacion;
  href: string;
}

const { meditacion, href } = Astro.props;
const totalEps = contarEpisodiosMeditacion(meditacion);
---

<a
  href={href}
  class="group block rounded-lg overflow-hidden"
>
  <div class="aspect-video bg-brand-500 overflow-hidden relative">
    {meditacion.thumbnail ? (
      <img
        src={meditacion.thumbnail}
        alt={meditacion.titulo}
        class="w-full h-full object-cover group-hover:scale-[1.08] transition-transform duration-[600ms] ease-[cubic-bezier(0.16,1,0.3,1)]"
        loading="lazy"
      />
    ) : (
      <div class="w-full h-full bg-gradient-to-br from-brand-600 to-brand-500"></div>
    )}
    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-11 h-11 bg-accent rounded-full flex items-center justify-center opacity-0 group-hover:opacity-100 scale-75 group-hover:scale-100 transition-all duration-300 z-10">
      <svg class="w-4 h-4 fill-brand-700 ml-0.5" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
    </div>
    <span class="absolute bottom-2 right-2 bg-black/70 backdrop-blur-sm text-brand-50 text-[11px] font-medium px-2 py-0.5 rounded z-10">
      {totalEps} eps
    </span>
  </div>
  <div class="pt-3 pb-1">
    <span class="text-[10px] font-semibold tracking-[0.12em] uppercase text-accent">
      Meditacion{meditacion.tipo ? ` ${meditacion.tipo}` : ''}
    </span>
    <h3 class="font-body text-sm font-semibold text-brand-50 group-hover:text-accent transition-colors duration-300 mt-0.5 line-clamp-2 leading-snug">
      {meditacion.titulo}
    </h3>
    <p class="text-xs text-brand-200 mt-1">{meditacion.guia}</p>
  </div>
</a>
