---
import Layout from '../../../layouts/Layout.astro';
import { getAllRutas, getRutaBySlug, getLibroBySlug, getMeditacionBySlug, getTemaBySlug, contarEpisodiosLibro, contarEpisodiosMeditacion } from '../../../lib/datos';
import type { Ruta } from '../../../lib/types';

export function getStaticPaths() {
  return getAllRutas().map(ruta => ({
    params: { ruta: ruta.slug },
  }));
}

const { ruta: rutaSlug } = Astro.params;
const ruta = getRutaBySlug(rutaSlug) as Ruta;
const temas = ruta.temas.map(s => getTemaBySlug(s)).filter(Boolean);

const pasos = ruta.pasos.map(paso => {
  if (paso.tipo === 'libro') {
    const libro = getLibroBySlug(paso.ref)!;
    return {
      ...paso,
      titulo: libro.titulo,
      subtitulo: libro.autor,
      thumbnail: libro.thumbnail,
      href: `/libros/${libro.slug}/`,
      episodios: contarEpisodiosLibro(libro),
      badge: 'Libro',
    };
  }
  const med = getMeditacionBySlug(paso.ref)!;
  return {
    ...paso,
    titulo: med.titulo,
    subtitulo: med.guia,
    thumbnail: med.thumbnail,
    href: `/meditaciones/${med.slug}/`,
    episodios: contarEpisodiosMeditacion(med),
    badge: 'Meditación',
  };
});
---

<Layout
  title={`${ruta.titulo} — Rutas de estudio — chuchurex`}
  description={ruta.descripcion}
>
  <section class="py-12 relative overflow-hidden">
    <div class="absolute top-1/2 right-0 -translate-y-1/2 w-64 h-64 border border-white/[0.03] rounded-full translate-x-1/2"></div>
    <div class="container mx-auto px-6 md:px-12 relative z-10">
      <nav class="text-sm text-brand-200 mb-5">
        <a href="/" class="hover:text-accent transition-colors duration-300">Inicio</a>
        <span class="mx-2 text-brand-200/30">/</span>
        <a href="/rutas/" class="hover:text-accent transition-colors duration-300">Rutas</a>
        <span class="mx-2 text-brand-200/30">/</span>
        <span class="text-brand-50">{ruta.titulo}</span>
      </nav>
      <div class="flex flex-wrap items-center gap-3 mb-3">
        {ruta.duracion_estimada && (
          <span class="text-[11px] text-brand-200">{ruta.duracion_estimada}</span>
        )}
        {temas.map(t => (
          <a href={`/temas/${t!.slug}/`} class="text-[11px] text-brand-200 hover:text-accent transition-colors duration-300">
            {t!.nombre}
          </a>
        ))}
      </div>
      <h1 class="text-3xl md:text-4xl font-display font-bold text-brand-50">{ruta.titulo}</h1>
      <p class="mt-3 text-brand-100 max-w-3xl leading-relaxed">{ruta.descripcion}</p>
      <div class="flex items-center gap-4 mt-4 text-[11px] text-brand-200 tracking-[0.1em] uppercase">
        <span>{ruta.pasos.length} {ruta.pasos.length === 1 ? 'paso' : 'pasos'}</span>
      </div>
    </div>
  </section>

  <section class="py-10 md:py-14">
    <div class="container mx-auto px-6 md:px-12 max-w-3xl">
      <div class="relative">
        <div class="absolute left-5 top-0 bottom-0 w-px bg-white/[0.06] md:left-6"></div>

        {pasos.map((paso, i) => (
          <div class:list={["relative pl-14 md:pl-16 pb-10", i === pasos.length - 1 && "pb-0"]}>
            <div class="absolute left-0 top-0 w-10 h-10 md:w-12 md:h-12 rounded-full bg-brand-600 text-brand-50 flex items-center justify-center font-display font-bold text-lg border-2 border-accent/30 z-10">
              {paso.orden}
            </div>

            <a
              href={paso.href}
              class="group block bg-brand-500 rounded-xl overflow-hidden border border-white/[0.04] hover:border-accent/20 hover:bg-brand-300 transition-all duration-400"
            >
              <div class="flex flex-col sm:flex-row">
                {paso.thumbnail && (
                  <div class="sm:w-40 sm:flex-shrink-0">
                    <img
                      src={paso.thumbnail}
                      alt={paso.titulo}
                      class="w-full h-32 sm:h-full object-cover group-hover:scale-105 transition-transform duration-700"
                      loading="lazy"
                    />
                  </div>
                )}
                <div class="p-4 flex-1">
                  <div class="flex items-center gap-2 mb-1">
                    <span class="text-[10px] font-semibold tracking-[0.12em] uppercase text-accent">{paso.badge}</span>
                    <span class="text-[10px] text-brand-200">{paso.episodios} eps</span>
                  </div>
                  <h3 class="font-display text-lg font-bold text-brand-50 group-hover:text-accent transition-colors duration-300 leading-snug">
                    {paso.titulo}
                  </h3>
                  <p class="text-sm text-brand-100 mt-1">{paso.subtitulo}</p>
                  {paso.nota && (
                    <p class="text-sm text-brand-200 mt-2 italic">{paso.nota}</p>
                  )}
                </div>
              </div>
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>
